{
  "name": "pi-code-tool-ts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Experimental TypeScript-first code-mode extension for Pi (scaffold only in M0.1)",
  "keywords": ["pi-package"],
  "pi": {
    "extensions": ["./src/extension.ts"]
  },
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "check": "bun run typecheck && bun test",
    "smoke:pi": "pi --offline --no-extensions -e ./src/extension.ts --list-models"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@types/bun": "^1.3.14",
    "typescript": "^5.8.3"
  }
}
